name: "Terraform"

on:
  workflow_dispatch

jobs:
  first_demo_job:
    runs-on: "ubuntu-latest"
    name: The first demo job
    steps:
      - name: Show the demo running
        env:
          VAR1: ${{ vars.TF_CLOUD_HOSTNAME }}
          VAR2: A Demo of
          VAR3: GitHub Actions
          VAR4: Workflow jobs
        run: |
          echo $VAR1 $VAR2 $VAR3 $VAR4.
    
